# 阶段 0.1 SGMII PHY 硬件契约闭合

## 状态

- 阶段：0.1
- 状态：PLANNED
- 执行边界：只读资料审阅和被动硬件观测
- 开始日期：2026-09-07
- 前置条件：阶段 0 已验收

## 计划

### 决策问题

在创建任何 XDC、IP 或 RTL 前，现有资料能否分别确认 M88E1111 的 SGMII 所接 MGT116 通道、TX/RX 差分极性、参考时钟频率，以及 PHY 的 strap、MDIO 地址、复位时序和 DB500 UDP Demo 的网络参数与校验规则？

### 最小证据

| 事实 | 最小权威证据 | 可接受结论 |
|---|---|---|
| SGMII 通道和极性 | 核心板与 MCON 板间连接器引脚映射、PCB 网表或明确标注的原理图页 | 已确认，或因资料缺失而阻塞 |
| GTX 参考时钟 | 原理图频率标注、BOM/晶振规格或板卡资料 | 已确认，或阻塞 |
| PHY strap、MDIO 和复位 | MCON 原理图和 M88E1111 数据手册 | 已确认，或阻塞 |
| 板卡版本 | 板卡丝印、BOM、版本标签或经核对的资料版本 | 已确认，或阻塞 |
| UDP Demo 网络与帧规则 | 已解除占用的 DB500 协议文档 | 已确认，或阻塞 |

### 允许操作

- 读取本地 Word/PDF、原理图文字和页面图像；在工作包 `work/` 内保存必要的临时渲染或摘录。
- 只读检查主机网络、JTAG 枚举、文件锁和 Git 状态。
- 更新本阶段相关 Markdown、运行工作包校验器、提交并推送过程记录。

### 禁止操作

- 不创建或修改 `fpga/` 下的 XDC、IP、RTL、工程 Tcl 或 Vivado 工程。
- 不下载 bitstream、不复位器件、不写 PHY/MDIO 寄存器、不启动 ILA/VIO。
- 不发送 ARP、ICMP、UDP 或其他主动网络报文。
- 不移动、修改或提交根目录的 Word/PDF 输入资料。

### 停止条件与恢复

当每个决策字段都有可追溯的“已确认”结论，或其缺失已经由最小证据明确为阻塞时，停止继续扩展资料搜集。本阶段不改变板卡或网络状态，无需硬件恢复；退出时关闭仅用于读取的文件句柄和工具会话。

## 执行记录

执行开始后仅记录实际采用的资料、命令和决定性发现；不得以计划内容替代证据。

## 验收标准

1. `BOARD_CONTRACT.md` 与 `INTERFACE_MATRIX.md` 中的 MGT116、参考时钟、PHY 管理和 UDP 协议字段逐项标注“已确认”或“BLOCKED”。
2. 每个“已确认”字段均能定位到输入文件、页码/章节或只读硬件观测；不得由网络名推断引脚、极性、频率或 MDIO 地址。
3. 所有未确认的硬件契约字段均列入 `OPEN-QUESTIONS.md`，并明确所需的下一份证据。
4. 未产生 `fpga/` 修改、配置下载或主动网络通信。
5. 相关工作包校验器通过，记录提交并推送到 `origin/main`。

## 验收结论

- 结论：待执行
- 结论日期：待执行
- 证据：待执行
