# 历史协作记录（2026-09-08 以前）

本目录是重构前 `AI-work` 的完整归档。内容包含旧的流程说明、工作包、诊断记录以及当时
复制到 `AI-work` 的日志/报告。它们保留原始结论和更正过程，用于审计和追溯，不是当前任务
的入口，也不代表当前模块状态。

当前开发请从 [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md) 开始；Vivado 的最新仿真、
构建和硬件产物只以 `fpga/led/` 下的原生目录为准。

归档内容：

- `HARDWARE_ENVIRONMENT-legacy.md`、`LOG-legacy.md`：旧全局硬件表和时间日志；
- `diagnostics/`：M88E1111 模式与管脚更正的详细证据；
- `work-packages/`：各次开发的原始计划、执行记录和复制的证据文件；
- `mode1/`、`mode2/`：旧架构预留目录。

历史记录中的相对路径按当时目录保存；若与当前工程路径不一致，以当前 `.xpr` 和原生
Vivado 目录为准。
